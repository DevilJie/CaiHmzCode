server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.31.7:3306/tech_share?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: caijie888***
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000
      pool-name: TechShareHikariCP

  data:
    redis:
      host: 192.168.31.7
      port: 6379
      password: 111111
      database: 0
      timeout: 10000ms
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0

# Actuator配置
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

# JWT配置
jwt:
  secret: TechShareSecretKeyForJwtTokenGeneration2024MustBeLongEnough
  expiration: 604800000  # 7天（毫秒）
  header: Authorization
  prefix: "Bearer "

# GitHub配置（用于获取README）
github:
  token:   # GitHub Personal Access Token，用于提高API调用限制

# 日志配置
logging:
  level:
    com.aifactory.techshare: debug
    com.baomidou.mybatisplus: debug
